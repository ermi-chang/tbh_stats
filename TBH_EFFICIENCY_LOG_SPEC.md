# TBH 効率ログ 1.0.0 機能仕様書

この文書は、TaskBarHero 周回効率計測アプリ「TBH 効率ログ」の機能、画面構成、内部構造、主要ボタン、データ仕様をまとめたものです。新しいUIを作成する際の機能説明・要件定義として使用することを目的とします。

---

## 1. アプリ概要

### 1.1 目的

TaskBarHero の放置/周回中に、ステージごとのゴールド効率を自動で記録し、どのステージが最も効率よく周回できているかを可視化する。

### 1.2 計測する主な値

- 現在所持ゴールド
- セッション開始後の増加ゴールド
- 経過時間
- 直近5分GPS
- 平均GPH
- 現在ステージの周回/h
- 1クリアごとのステージ履歴
- ステージ別ハイスコア
- オススメステージ

### 1.3 方針

- ゲーム通信の復号、改ざん、自動操作は行わない。
- Steam APIやゲームサーバーへの追加リクエストは行わない。
- 画面OCRと画像認識だけでローカル計測する。
- ステージ履歴は「1ステージクリア = 1行」で記録する。
- 通常時は重いOCRを連打せず、右下ゲージの軽量画像判定を中心にする。
- ステージ番号は `1-1〜3-10` の範囲で扱い、OCRの `1/7` 誤読は直近ステージ文脈で補正する。
- 所持ゴールドが減少した周回は、支出またはOCR揺れの影響を受けるため効率集計から除外する。

---

## 2. 技術構成

### 2.1 実装

- Python
- PySide6
- mss
- Pillow
- OpenCV
- numpy
- pytesseract
- SQLite

### 2.2 外部依存

- Tesseract OCR本体
  - 標準想定パス: `C:\Program Files\Tesseract-OCR\tesseract.exe`

### 2.3 保存ファイル

- `config.json`
  - OCR設定、言語設定、自動ROI設定、Tesseractパスを保存
- `data/tbh_ocr_stats.sqlite3`
  - セッション、所持金サンプル、ステージ履歴、ステージ別リセット情報を保存
- `exports/`
  - CSV出力先
- `logs/`
  - デバッグログ用

---

## 3. 画面構成

### 3.1 共通ヘッダー

表示内容:

- アプリ名: `TBH 効率ログ 1.0.0`
- 状態バッジ
  - 待機
  - 計測中
- 開始ボタン
- 停止ボタン

ボタンルール:

- 開始: オレンジ系、計測開始
- 停止: グレー系、計測停止
- 保存/CSV/リセット系: 茶色またはグレーで統一

---

## 4. 計測タブ

### 4.1 役割

通常使用時に見るメイン画面。現在の効率、オススメ、ステージ履歴を確認する。

### 4.2 表示カード

#### 所持金

現在OCRで読めている所持ゴールド。

#### 増加G

セッション開始時からの増加ゴールド。

#### 経過

セッション開始からの経過時間。
停止中は進まない。

#### 直近5分GPS

直近5分の所持金サンプルから算出したGPS。

#### 平均GPH

セッション全体の平均GPH。

#### 今ステージ周回/h

最後に取得したステージ秒数から逆算した1時間あたり周回数。

表示例:

```text
1-9  24.5回
```

#### オススメ

効率表のハイスコア基準で最も良いステージを表示する。

表示形式:

```text
ステージ / GPS / GPH / 周回数
```

例:

```text
1-8 / GPS 7.91 / GPH 28,478 / 9周
```

### 4.3 ステージ履歴

1クリアごとに1行表示する。

列:

- ステージ
- 秒
- 増加G
- GPS
- GPH
- 周回/h

計算式:

```text
増加G = ステージ終了時ゴールド - ステージ開始時ゴールド
GPS = 増加G / 秒
GPH = GPS * 3600
周回/h = 3600 / 秒
```

### 4.4 ステージ履歴ボタン

#### CSV

現在セッションのステージ履歴と所持金サンプルをCSV出力する。

出力例:

- `exports/stage_runs_<session_id>.csv`
- `exports/money_samples_<session_id>.csv`

#### リセット

現在セッションのステージ履歴をリセットする。

---

## 5. 効率表タブ

### 5.1 役割

計測タブとは切り分けた、ステージ別ハイスコア管理画面。

### 5.2 対象ステージ

固定で以下を表示する。

- 1-1 ～ 1-10
- 2-1 ～ 2-10
- 3-1 ～ 3-10

### 5.3 表示列

- ステージ
- 最高GPS
- 最高GPH
- 秒
- 増加G
- 採用件数/全件数

### 5.4 ハイスコア判定

基本は各ステージの最高GPHを採用する。

ただし、同一ステージのログが10件以上ある場合は、異常に低い/高い外れ値を除外してからハイスコアを判定する。

目的:

- OCR誤読による異常値をオススメに採用しない
- 極端な低効率ログで平均値を壊さない
- 実際に安定して高効率だったステージを残す

### 5.5 リセット

#### 選択ステージリセット

選択したステージのハイスコア履歴だけをリセットする。

#### 全リセット

全ステージのハイスコア履歴をリセットする。

---

## 6. 範囲・OCRタブ

### 6.1 役割

OCR対象、言語、Tesseractパス、プレビュー、調整値を管理する。

### 6.2 言語設定

#### UI言語

アプリ表示用の言語。

対応:

- 日本語
- English
- 中文

#### ゲーム言語

TaskBarHero側の表示言語。OCRプロファイル用。

対応:

- 日本語
- English
- Deutsch
- Español
- Français
- Polski
- Português-Brasil
- Русский
- Türkçe
- Українська
- 简体中文
- 繁體中文
- 한국어
- ภาษาไทย
- Tiếng Việt
- Bahasa Indonesia

注意:

UI言語とゲーム言語は一致しなくてよい。

例:

```text
UI言語: 日本語
ゲーム言語: English
```

### 6.3 対象画面

mssで取得できるモニター一覧から、TaskBarHeroが表示されている画面を選ぶ。

### 6.4 自動設定

ゲーム画面を表示した状態で押す。

処理内容:

1. 左上ゴールドアイコンを検出
2. ゲーム画面倍率を推定
3. 所持金ROIを自動計算
4. ステージROIを自動計算
5. 秒数ROIを自動計算
6. 通知全文ROIを自動計算
7. 右下ボスアイコンを検出
8. 進捗ゲージROIを自動計算

対応ゲーム内スケール:

- x1
- x1.25
- x1.5
- x2
- x3

自動設定後はポップアップを出さず、ステータスバーに結果だけ表示する。

### 6.5 OCR対象切替

対象ボタン:

- 所持金
- ステージ
- 秒数
- ゲージ
- 通知全文

現在選択中の対象だけ、必要な調整項目を表示する。

#### 所持金選択時

表示する設定:

- 所持金拡大
- 所持金しきい値
- 所持金反転
- 所持金更新
- 軽量モード

#### ステージ選択時

表示する設定:

- 通知系拡大
- 通知系しきい値
- 通知反転

#### 秒数選択時

表示する設定:

- 通知系拡大
- 通知系しきい値
- 通知反転

#### 通知全文選択時

表示する設定:

- 通知系拡大
- 通知系しきい値
- 通知反転

#### ゲージ選択時

OCR設定は表示しない。
ゲージ状態、fill率、blue率、purple率、ボス基準情報だけをプレビュー/ログに出す。

ver.28修正:

- ゲージ選択時に `所持金更新` が表示されないようにする。

### 6.6 プレビュー

#### 元画像

現在選択しているROIの元画像を表示する。

#### OCR処理後

しきい値、反転、拡大などを適用した後の画像を表示する。

ゲージ対象の場合はOCR画像ではなくゲージ判定用の処理結果を表示する。

### 6.7 OCRテスト

現在のROIと設定で、所持金・ステージ・秒数・ゲージ状態をテストする。

ログ表記ルール:

- 最終的に採用した値を先頭に出す
- raw文字列は補足として後ろに出す

例:

```text
秒=148 raw:1488
```

避ける表記:

```text
秒数(148)->148
秒数14842->14842
秒数(14842->14842
```

---

## 7. 自動ROIの基準

### 7.1 左上ゴールドアイコン

所持金、通知、ステージ、秒数の基本基準。

検出方式:

- テンプレート照合
- x1 / x1.25 / x1.5 / x2 / x3 の候補倍率で探索
- 検出後、ゲーム倍率を `detected_game_scale` として保存

### 7.2 右下ボスアイコン

進捗ゲージの基準。

理由:

- 右下ゲージは画面位置やゲーム倍率によってズレる
- ボスアイコンから相対的に取る方が安定する

注意:

青ゲージ時にボスアイコンが光るため、完全一致ではなくグレースケール/エッジ寄りの照合を使う。

---

## 8. ゲージ判定仕様

### 8.1 目的

ステージクリア/失敗の区切りを判定する。

### 8.2 状態

- purple
- blue
- blue_reached
- unknown/reset

### 8.3 状態遷移

```text
purple検出
→ ステージ開始
→ start_gold取得

blue進行
→ 継続

blue_reached検出
→ クリア候補

blue_reached後にpurple/resetへ戻る
→ 1ステージ終了
→ 終了OCR
→ ステージ履歴へ1行記録
```

### 8.4 可変ゲージ監視

ゲージ監視は固定間隔ではなく、状況に応じて間隔を変える。

```text
0〜70%付近: 10秒
71〜90%付近: 3.5秒
青ゲージ中/到達時: 1.5秒
```

目的:

- クリアから遠い時は軽くする
- クリア間際だけ細かく監視する
- OCRを連打せずPC負荷を下げる

---

## 9. OCR仕様

### 9.1 所持金OCR

対象:

- 左上の所持金数字のみ

処理:

- 数字・カンマ優先
- 白黒化
- 反転対応
- 誤読補正
- 前回値から大きく下がる異常値は無視

### 9.2 ステージOCR

対象:

- `1-5` のような数字とハイフン

言語非依存化のため、全文ではなく数字部分だけ読む。

### 9.3 秒数OCR

対象:

- `82` のような数字

補正例:

```text
1488 → 148
14842 → 148
821053 → 82
```

補正方針:

- OCR結果だけでなく、ゲージ開始〜終了の実測時間に近い候補を優先する。
- 常識的な周回秒数範囲から外れる値は疑う。

### 9.4 通知全文OCR

通常計測では使用しない。
デバッグ/保険扱い。

---

## 10. ステージ判定と計算

### 10.1 1ステージ開始

ゲージがpurple状態になった時点で開始候補。

処理:

- start_timeを記録
- start_goldをOCRまたは直近所持金から取得

### 10.2 1ステージ終了

blue_reached後にpurple/resetへ戻った時点で終了。

処理:

- end_timeを記録
- end_goldをOCR
- ステージ番号をOCR
- 秒数をOCR
- 1ステージ履歴を記録

### 10.3 ステージ履歴の計算

```text
gain_gold = end_gold - start_gold
GPS = gain_gold / duration_sec
GPH = GPS * 3600
round_per_hour = 3600 / duration_sec
```

### 10.4 左下ステージ表示の注意

左下表示は現在ステージではなく次ステージ扱い。

例:

```text
左下表示: 1-5
現在ステージ: 1-4
```

必要な場合は、内部で1ステージ戻して扱う。

---

## 11. 重複・分離ログ対策

### 11.1 目的

1周分のゴールド取得が複数ログに分離する問題を抑える。

例:

```text
344 + 1505
294 + 1580
344 + 1510
```

### 11.2 原因

- ゲージ取得間隔が長すぎる
- 終了検知と所持金OCRのタイミングがズレる
- 1周分の報酬が複数回のゴールド増加として見える

### 11.3 対策

- ゲージ監視を可変間隔にする
- 青ゲージ中/到達時は1.5秒監視
- ステージ履歴は通常OCR tickでは作らず、ゲージ終了時だけ作る
- 同ステージ・同秒数・短時間の連続ログは分割候補として合算する

---

## 12. データベース仕様

### 12.1 samples

所持金OCRのサンプルを保存。

列:

- id
- session_id
- ts
- ts_iso
- money
- raw_text
- accepted
- note

### 12.2 sessions

計測セッションを保存。

列:

- session_id
- started_at
- ended_at
- start_money
- end_money
- gain
- elapsed
- avg_mps
- avg_mph

内部名は mps/mph のままだが、UI表示上は GPS/GPH として扱う。

### 12.3 stage_runs

ステージクリア履歴を保存。

列:

- id
- session_id
- ts
- ts_iso
- stage
- duration_sec
- money
- money_delta
- mps
- mph
- raw_text

内部名は mps/mph のままだが、UI表示上は GPS/GPH として扱う。

### 12.4 stage_highscore_resets

ステージ別ハイスコアの個別リセット時刻を保存。

列:

- stage
- reset_after_ts
- reset_at_iso

---

## 13. CSV仕様

### 13.1 ステージ履歴CSV

出力ファイル:

```text
exports/stage_runs_<session_id>.csv
```

主な列:

- session_id
- ts_iso
- stage
- duration_sec
- money
- money_delta
- gps
- gph
- raw_text

### 13.2 所持金サンプルCSV

出力ファイル:

```text
exports/money_samples_<session_id>.csv
```

主な列:

- session_id
- ts_iso
- money
- raw_text
- accepted
- note

---

## 14. 設定保存仕様

### 14.1 config.jsonに保存するもの

- tesseract_path
- UI言語
- ゲーム表示言語
- OCR方式
- 所持金OCR設定
  - 拡大
  - しきい値
  - 反転
  - 所持金更新間隔
- 通知系OCR設定
  - 拡大
  - しきい値
  - 反転
- 自動ROI情報
- 検出ゲーム倍率
- 軽量モード

### 14.2 アップデート時の注意

アプリを新規フォルダに展開すると、`config.json` が引き継がれない場合がある。

推奨運用:

- 固定フォルダに上書き
- `config.json` と `data/` は残す

---

## 15. ボタン一覧

### 15.1 共通ヘッダー

| ボタン | 役割 |
|---|---|
| 開始 | 計測開始 |
| 停止 | 計測停止 |

### 15.2 計測タブ

| ボタン | 役割 |
|---|---|
| CSV | ステージ履歴と所持金サンプルをCSV出力 |
| リセット | 現在セッションのステージ履歴をリセット |

### 15.3 効率表タブ

| ボタン | 役割 |
|---|---|
| 選択ステージリセット | 選択ステージのハイスコアをリセット |
| 全リセット | 全ステージのハイスコアをリセット |

### 15.4 範囲・OCRタブ

| ボタン | 役割 |
|---|---|
| 自動設定 | ゴールドアイコン、ボスアイコン、各ROIを自動検出 |
| OCRテスト | 現在の設定でOCR/ゲージ判定をテスト |
| 参照 | tesseract.exeを指定 |
| プレビュー更新 | 現在の対象ROIを再表示 |

### 15.5 対象切替ボタン

| ボタン | 役割 |
|---|---|
| 所持金 | 所持金ROIと所持金OCR設定を表示 |
| ステージ | ステージ番号ROIと通知系OCR設定を表示 |
| 秒数 | 秒数ROIと通知系OCR設定を表示 |
| ゲージ | ゲージROIとゲージ状態を表示 |
| 通知全文 | 通知全文ROIと通知系OCR設定を表示 |

---

## 16. UI再設計時の要点

新UIを作る場合、以下を守る。

### 16.1 優先表示

計測タブでは、ユーザーが判断に使う値だけを表示する。

優先:

- オススメ
- 所持金
- 増加G
- 直近GPS
- 平均GPH
- 今ステージ周回/h
- ステージ履歴

非優先:

- OCR raw
- しきい値
- ROI詳細
- デバッグ文字列

### 16.2 設定は必要な時だけ表示

対象が所持金なら所持金用設定だけ表示する。ゲージならOCR設定は出さない。

### 16.3 ステージ履歴と効率表を分ける

- ステージ履歴: 今の計測セッションの1クリアごとのログ
- 効率表: 長期的なステージ別ハイスコア管理

### 16.4 処理負荷を増やさない

- OCRを常時連打しない
- ゲージ画像処理を主軸にする
- クリア付近だけ高頻度監視する
- 経過時間表示はOCRと切り離す

---

## 17. 禁止・非対応

- ゲーム通信の復号
- ゲーム通信の改ざん
- 自動操作
- 自動売買
- 自動出品
- Steamログイン情報の保存
- MITM/証明書差し替え
- BAN回避機能

---

## 18. 今後の拡張候補

- UI全面リニューアル
- ステージ別グラフ
- セッション比較
- OCR精度診断モード
- 画像テンプレート再学習
- 言語別通知バー比率プロファイルの強化
- 異常値除外ルールのUI調整
- ステージ別メモ欄



---

## ver.30 追加仕様

### ステージOCR補正

- 対応ステージ範囲は `1-1〜3-10`。
- `2-1` が `2-7` と読まれるような誤読に対し、直近で記録したステージおよび次ステージ候補を使って補正する。
- 補正は保守的に行い、直近候補と一致する場合、または同一ワールド内で大きくジャンプする読み取りになった場合のみ適用する。

### ゴールド減少時の扱い

- 所持ゴールドが前回OCR値より減少した場合、現在所持金は更新する。
- ただし、そのステージ周回は正しい獲得ゴールドを算出できないため、ステージ効率ログには採用しない。
- 追加G / GPS / GPH はマイナス表示しない。
- 減少検出後は、現在所持金を新しい基準として次の周回計測を継続する。

## ver.32 補足
- 計測タブのオススメ表示は、対象ステージの累計ログ件数ではなく、採用ハイスコア行の `3600 / 秒` から算出した周回/hを表示する。
- 右下ゲージの基準検出は、ボスアイコンの完全一致ではなく、エッジ・前景色・赤アクセント・白黒構造を総合評価して明るい背景での誤検出を抑制する。
